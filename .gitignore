# 依赖（Render/Railway 会自动 npm install）
node_modules/

# 本地密钥 / 环境变量（切勿提交）
.env
.env.local
*.local

# 日志
*.log
npm-debug.log*

# 运行时访问统计（Render 临时盘，不提交；部署时由 server 自动从 0 初始化）
stats.json

# 系统文件
.DS_Store
Thumbs.db
